/**
 * ECOS Popover — enterprise standard for floating overlay panels.
 *
 * Built on Radix UI Popover which renders via Portal natively, ensuring it is
 * never clipped by Dialog, Drawer, or Sheet. Supports auto-flip and collision
 * detection out of the box.
 *
 * Use for tooltips, detail panels, and any floating UI that is NOT a select/combobox.
 * For searchable selects, use EcosCombobox. For static option lists, use EcosSelect.
 */
export { Popover, PopoverAnchor, PopoverContent, PopoverTrigger } from '@/components/ui/popover';
