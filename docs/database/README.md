# Database Documentation

Data model and persistence documentation for ECOS ERP: schema, entity
relationships, migration conventions, and indexing strategy.

**Planned**
- Entity-Relationship Diagrams (ERD) per module
- Schema reference & data dictionary
- Migration & seeding conventions
- Indexing and performance notes

_Current schema = Laravel defaults only (`users`, `cache`, `jobs`). This
directory is a placeholder for future, module-driven schema docs._
