# Enums

**Namespace:** `App\Enums`

Application-wide PHP 8.1+ backed enums (statuses, types, roles, etc.) that are
not owned by a single module.

**Convention:** `PascalCase` singular name, suffixed where it adds clarity
(e.g. `OrderStatus`, `PaymentMethod`). Cases are `PascalCase`. Prefer **backed
enums** (`string`/`int`) for persistence and serialization.

_Placeholder — no enums implemented yet (engineering foundation, no business logic)._
