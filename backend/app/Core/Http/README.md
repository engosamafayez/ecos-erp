# Core\Http

**Namespace:** `App\Core\Http`

Reusable HTTP building blocks for the Core Framework — base controllers, base
form requests, shared middleware, and request/response helpers that every
module's `Presentation` layer can extend.

Pairs with:
- [`App\Core\Responses\ApiResponse`](../Responses/ApiResponse.php) — standardized JSON envelope
- [`App\Traits\HasApiResponse`](../../Traits/HasApiResponse.php) — controller convenience trait

_Placeholder — no HTTP base classes implemented in Core Framework v1
(no business logic, no controllers yet)._
